# Проект Sprint_5: автоматизация тестирования Stellar Burgers

##  Рекомендация к файлу conftest.py "Нужно исправить: регистрация через пользовательский интерфейс ненадежна и медленна для фикстуры. Для создания тестового пользователя или заранее подготовленного пользователя следует использовать API" Я пока не умею писать автоматизированные тесты с API, эта тема вроде бы будет в 7 спринте. Сделала отдельный файл для тестового пользователя и фикстуру
## Test_constructor - при использовании стандартного click() появляется ошибка ElementClickInterceptedException на вкладке "Булки". Чтобы тест проходил, можно применить JavaScript-клик.  
## test_logout исправлен убрала лишнюю проверку

## Описание
Автотесты на Selenium + pytest для проверки функциональности сайта Stellar Burgers:
- регистрация, вход (4 способа), личный кабинет, конструктор (вкладки), выход.

## Технологии
- Python 3.14
- Selenium
- pytest
- webdriver-manager

## Установка и запуск
1. Клонировать репозиторий.
2. Создать виртуальное окружение: `python -m venv .venv`
3. Активировать: `.venv\Scripts\activate.bat`
4. Установить зависимости: `pip install -r requirements.txt`
5. Запустить тесты: `pytest tests/ -v`

## Структура
- `conftest.py` – фикстуры (драйвер, новый пользователь)
- `locators.py` – локаторы элементов
- `generators.py` – генерация уникального email
- `data.py` – тестовые данные
- `tests/` – все тесты (по 1 файлу на функциональность):
    test_registration.py – регистрация (успех + ошибка пароля)
    test_login.py – 4 способа входа
    test_personal_account.py – переход в личный кабинет
    test_constructor.py - – переход к разделам 
    test_logout.py – выход из аккаунта# Sprint_5
    test_go_constructor_from_personal_account  - переход по клику на конструктор
    test_go_to_main_via_logo - переход по клику на логотип
